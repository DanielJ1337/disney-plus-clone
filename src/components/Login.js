import React from "react";

import styled from "styled-components";

export const Login = () => {
  return <Container>Login</Container>;
};

const Container = styled.div`
  position: relative;
  height: calc(100vh-4.375rem);

  background-image: url("/images/disney-plus-login.jpg");
`;
